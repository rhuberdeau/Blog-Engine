<h1>Admin Page</h1>
<%= form_tag publish_articles_path, method: :put do %>
	<table>
		<tr>
			<th>Published</th>
			<th>Title</th>
		</tr>
		<% @articles.each do |article| %>
			<tr class="unpublished-article">
				<td><%= check_box_tag "article_ids[]", article.id, false, class: "publish"  %></td>
				<td><%= article.title %></td>
			</tr>
	  <% end %>
	</table>
	<%= submit_tag "Publish Articles" %>
<% end %>